This folder is reserved for the SCOS Recovery image.

SCOSRecovery.wim is not stored in this repository because it exceeds GitHub's normal file size limit.

When recovery support is enabled, SCOS Builder will provide or download the recovery image and copy it into this folder during ISO creation.